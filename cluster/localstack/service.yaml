apiVersion: v1
kind: Service
metadata:
  name: localstack
  labels:
    app: localstack
spec:
  type: NodePort
  ports:
  - port: 4566
    targetPort: 4566
    nodePort: 30566
    name: api
  selector:
    app: localstack
